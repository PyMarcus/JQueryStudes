//ao carregar uma página, um codigo js comum é executado apenas quando body.onload() captura o evento

// com jquery, pode-se usar o document. ready

$(document).ready(function(){

    //aqui dentro, vem toda codificação que se quer executar após o carregamento da página. wrapper executa a funcao e passa o retorno
    //alert("OU");

})